# Dart SDKs

- `auth/`: migrated 2Finance auth SDK.
- `blockchain/`: migrated 2Finance blockchain client.
- `sdk/`: unified Dart facade for auth, network, analytics, orchestrator, MCP,
  planner, trading control, keystore, hummingbot, and providers.

These packages remain independent Dart packages so existing mobile/app imports can
migrate gradually.
